server:
  port: 16200

spring:
  profiles: dev
  cloud:
    config:
      discovery:
        enabled: true
      server:
        git:
          uri: https://github.com/luotongxi/lala-cloud-config.git
          
eureka:
  instance:
    hostname: configserver
    perferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://discovery:16200/eureka/

user:
  username: luotong
  password: 111111

---
server:
  port: 16200
  
spring:
  profiles: test
  cloud:
    config:
      server:
        git:
          uri: https://github.com/luotongxi/lala-cloud-config.git
          
eureka:
  instance:
    hostname: configserver
    perferIpAddress: true
  client:
    registerWithEureka: true
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://discovery:16200/eureka/
