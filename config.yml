server:
  port: 16200

spring:
  profiles: dev
  cloud:
    config:
      discovery:
        enabled: true
      server:
        git:
          uri: https://github.com/luotongxi/lala-cloud-config.git

user:
  username: luotong
  password: 111111

---
server:
  port: 16100
  
spring:
  profiles: test
  cloud:
    config:
      server:
        git:
          uri: https://github.com/luotongxi/lala-cloud-config.git
